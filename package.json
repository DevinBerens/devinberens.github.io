{
  "name": "recipe-box",
  "homepage": "https://devinberens.github.io",
  "version": "1.0.0",
  "private": true,
  "description": "A shared recipe app. Add cooks to a group, then add recipes by snapping a photo of a recipe-book page (in-browser OCR) or typing them in. Synced across phones via Firebase.",
  "scripts": {
    "start": "npx serve ."
  }
}
